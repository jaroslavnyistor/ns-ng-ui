/*
 * Public API Surface of nikisoft-ng-ui
 *
 * To generate all public-api as one index.ts do following:
 * 1. npm install create-ts-index --save-dev
 * 2. Open terminal at src folder of library
 * 3. Run cti entrypoint -n ./lib
 * 4. Rename file entrypoint.ts to index.ts
 * 5. Added here export * from './lib/index';
 */
export * from './lib/index';
